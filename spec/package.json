{
	"name": "@wikimedia/servicelib-node-spec",
	"version": "1.0.1",
	"description": "Generate a single OpenAPI v3 spec from multiple sources",
	"main": "index.js",
	"bin": {
		"servicelib-spec": "index.js"
	},
	"scripts": {
		"test": "npm run lint && mocha ./test",
		"lint": "eslint --max-warnings 0 --ext .js --ext .json .",
		"lint:fix": "eslint --fix ."
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"chai": "^4.3.4",
		"express": "^4.17.1",
		"js-yaml": "^4.1.0",
		"json-refs": "^3.0.15",
		"swagger-jsdoc": "^6.1.0",
		"swagger-ui-express": "^4.1.6",
		"yamljs": "^0.3.0"
	},
	"devDependencies": {
		"eslint-config-wikimedia": "^0.21.0",
		"mocha": "^9.1.3"
	},
	"engines": {
		"node": ">=12"
	},
	"repository": {
		"type": "git",
		"url": "https://gerrit.wikimedia.org/r/plugins/gitiles/mediawiki/services/servicelib-node/",
		"directory": "spec/"
	}
}
